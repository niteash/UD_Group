# Required for /api/chat (Vercel Edge Function calling the Anthropic API).
# Get a key at https://console.anthropic.com — set this in your Vercel
# project's Environment Variables, do not commit the real value.
ANTHROPIC_API_KEY=
